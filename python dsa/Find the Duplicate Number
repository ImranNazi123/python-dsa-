class Solution:
    def findDuplicate(self, nums: List[int]) -> int:
        # time:0(n); wc:0(n-1); space:0(1)
        while True:
            x=nums[0]
            if nums[x]==x:
                return x
            # swap
            nums[0],nums[x]=nums[x],nums[0]
            
